sudo: required
services:
  - docker

before_install:
  - docker build -t dandresavid/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run dandresavid/react-test npm test -- --coverage

after_success:
  - docker build -t dandresavid/multi-client ./client
  - docker build -t dandresavid/multi-nginx ./nginx
  - docker build -t dandresavid/multi-server ./server
  - docker build -t dandresavid/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push dandresavid/multi-client
  - docker push dandresavid/multi-nginx
  - docker push dandresavid/multi-server
  - docker push dandresavid/multi-worker
